// If your package names don't match ours, you only need to update them here.
import VM from 'scratch-vm';
import Renderer from 'scratch-render';
import AudioEngine from 'scratch-audio';
import Storage from '@sailfish-studio/scratch-storage';
import SVGRenderer from '@sailfish-studio/scratch-svg-renderer';
import JSZip from '@sailfish-studio/jszip';

export default {
  VM,
  Renderer,
  AudioEngine,
  Storage,
  SVGRenderer,
  JSZip
};
