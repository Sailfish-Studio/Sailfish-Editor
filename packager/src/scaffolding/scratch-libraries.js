// If your package names don't match ours, just update them here and it will all just work

import VM from 'scratch-vm';
import Renderer from 'scratch-render';
import ScratchStorage from '@sailfish-studio/scratch-storage';
import AudioEngine from 'scratch-audio';
import {BitmapAdapter} from '@sailfish-studio/scratch-svg-renderer';

export {
    VM,
    Renderer,
    ScratchStorage,
    AudioEngine,
    BitmapAdapter
};
